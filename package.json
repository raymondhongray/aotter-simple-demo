{
  "name": "aotter-simple-demo",
  "version": "1.0.0",
  "description": "aotter simple demo",
  "author": "Raymond.Hong",
  "private": true,
  "scripts": {
    "test": "jest",
    "dev": "cross-env BASE_URL=http://localhost:3000 NUXT_PORT=3000 NODE_ENV=development nodemon server/index.js --watch server",
    "build": "nuxt build",
    "start": "cross-env NODE_ENV=production node server/index.js",
    "generate": "nuxt generate",
    "heroku-postbuild": "npm run build"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.5.4",
    "cross-env": "^5.2.0",
    "express": "^4.16.4",
    "nuxt": "^3.4.3",
    "vue-plyr": "^5.1.3"
  },
  "devDependencies": {
    "tailwindcss": "^1.0.0",
    "@vue/test-utils": "^1.0.0-beta.27",
    "babel-core": "7.0.0-bridge.0",
    "babel-jest": "^24.1.0",
    "jest": "^24.1.0",
    "vue-jest": "^3.0.3",
    "nodemon": "^1.18.9"
  }
}
